const SnapHelp = {
  ls: "list all files and directories within current file system",
  pwd: "writes the full pathname of the current working directory",
  cd:
    "used to change directories by specificying folder to change into or ../ to move back to parent directory",
  whim:
    "whim is a txt file reader. using the command followed by the file opens it up in the terminal",
  clear: "will clear the terminal of all output",
  "snap --help": "gives the list of current working commands in snap",
};

export default SnapHelp;
